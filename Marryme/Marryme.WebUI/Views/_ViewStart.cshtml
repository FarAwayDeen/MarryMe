@{ if (Session["Role"] != null && (Session["Role"]).ToString() == "Registrar")
    {
        Layout = "~/Views/Shared/_RegistrarLayout.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }
}
